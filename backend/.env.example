DEBUG=True
API_PORT=8000
# Signs sign-in tokens. Generate one with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
# Supabase: Project Settings > Database > Connection string (Session pooler, URI format)
DATABASE_URL=
