DEBUG=True
API_PORT=8000
# Signs sign-in tokens. Generate one with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
# Supabase: Connect > Session pooler connection string (URI format)
DATABASE_URL=
# Comma-separated sites allowed to call the API, e.g. https://your-site.onrender.com
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Portkey AI gateway key (not used by the app yet)
PORTKEY_API_KEY=
